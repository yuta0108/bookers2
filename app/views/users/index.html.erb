<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'layouts/info', user:@user %>
      <%= render 'layouts/body', book:@book %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th></th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
          <tr>
            <td><%= user.image do %><%= image_tag user.get_profile_image(100,100) %><% end %></td>
            <td><%= user.name %></td>
            <td><%= link_to "show", user_path %></td>
            <td></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
